<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        <p class="text-center">Chào mừng bạn đến với hệ thống tính phí bảo hiểm xã hội.</p>

        <% if (user) { %>
            <p class="text-center">Xin chào, <strong><%= user.username %></strong>! </p>
            <div class="links-group">
                <a href="/dashboard" class="button btn-primary">Dashboard</a>
                <a href="/logout" class="button btn-secondary">Đăng xuất</a>
            </div>
        <% } else { %>
            <div class="links-group">
                <a href="/register" class="button btn-primary">Đăng ký</a>
                <a href="/login" class="button btn-secondary">Đăng nhập</a>
            </div>
        <% } %>

        <% if (typeof message !== 'undefined' && message) { %>
            <p class="message"><%= message %></p>
        <% } %>
        <% if (typeof error !== 'undefined' && error) { %>
            <p class="error"><%= error %></p>
        <% } %>

    </div>
</body>
</html>